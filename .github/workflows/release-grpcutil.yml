name: Release grpcutil

on:
  push:
    branches:
      - main
    paths:
      - "grpcutil/**"

permissions:
  contents: write

jobs:
  release:
    uses: ./.github/workflows/deploy-go-module.yml
    with:
      module_path: grpcutil
      bump_type: patch
    secrets: inherit
