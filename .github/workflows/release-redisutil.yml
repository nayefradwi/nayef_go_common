name: Release redisutil

on:
  push:
    branches:
      - main
    paths:
      - "redisutil/**"

permissions:
  contents: write

jobs:
  release:
    uses: ./.github/workflows/deploy-go-module.yml
    with:
      module_path: redisutil
      bump_type: patch
    secrets: inherit
