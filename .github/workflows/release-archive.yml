name: Release archive

on:
  push:
    branches:
      - main
    paths:
      - "archive/**"

permissions:
  contents: write

jobs:
  release:
    uses: ./.github/workflows/deploy-go-module.yml
    with:
      module_path: archive
      bump_type: patch
    secrets: inherit
