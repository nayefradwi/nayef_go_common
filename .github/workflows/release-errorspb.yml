name: Release errorspb

on:
  push:
    branches:
      - main
    paths:
      - "errorspb/**"

permissions:
  contents: write

jobs:
  release:
    uses: ./.github/workflows/deploy-go-module.yml
    with:
      module_path: errorspb
      bump_type: patch
    secrets: inherit
