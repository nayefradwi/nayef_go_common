name: Release httputil

on:
  push:
    branches:
      - main
    paths:
      - "httputil/**"

permissions:
  contents: write

jobs:
  release:
    uses: ./.github/workflows/deploy-go-module.yml
    with:
      module_path: httputil
      bump_type: patch
    secrets: inherit
