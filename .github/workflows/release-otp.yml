name: Release otp

on:
  push:
    branches:
      - main
    paths:
      - "otp/**"

permissions:
  contents: write

jobs:
  release:
    uses: ./.github/workflows/deploy-go-module.yml
    with:
      module_path: otp
      bump_type: patch
    secrets: inherit
