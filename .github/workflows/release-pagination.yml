name: Release pagination

on:
  push:
    branches:
      - main
    paths:
      - "pagination/**"

permissions:
  contents: write

jobs:
  release:
    uses: ./.github/workflows/deploy-go-module.yml
    with:
      module_path: pagination
      bump_type: patch
    secrets: inherit
