name: CI redisutil

on:
  push:
    branches:
      - main
    paths:
      - "redisutil/**"
  pull_request:
    paths:
      - "redisutil/**"

jobs:
  ci:
    uses: ./.github/workflows/ci-go-module.yml
    with:
      module_path: redisutil
