name: CI httputil

on:
  push:
    branches:
      - main
    paths:
      - "httputil/**"
  pull_request:
    paths:
      - "httputil/**"

jobs:
  ci:
    uses: ./.github/workflows/ci-go-module.yml
    with:
      module_path: httputil
